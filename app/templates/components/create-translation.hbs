{{yield}}


<div class="form-horizontal">
  <div class="form-group">
      <label class="col-sm-2 control-label"></label>
      <div class="col-sm-10">
        {{paper-input label="Phrases etc 'Hello,Goodbye'" value=model.name}}
      </div>
  </div>

  <div class="form-group">
      <label class="col-sm-2 control-label"></label>
      <div class="col-sm-10">
        {{paper-input label="Category , etc 'Greetings'" value=model.category}}
      </div>
  </div>
    <!-- <div class="form-group">
        <label class="col-sm-2 control-label"></label>
        <div class="col-sm-10">
          {{paper-input label="Translation Word" value=name}}
        </div>
    </div>
    <div class="form-group">
        <label class="col-sm-2 control-label">Language</label>
        <div class="col-sm-10">
          {{#paper-select disabled=disableSelect placeholder="Languages" }}
            {{#each model as |translate|}}
              {{#paper-option value=translate.language}}
                {{translate.language}}
              {{/paper-option}}
            {{/each}}

          {{/paper-select}}
               </div>
    </div> -->

    <div class="form-group">
        <div class="col-sm-offset-2 col-sm-10">
          <button type="submit" class="md-button Pastelgreen" {{action 'newTranslation' model}}>Create phrase</button>
          <!-- {{#paper-button raised=true action="buttonClicked"}}Contribute{{/paper-button}} -->
            <!-- <button type="submit" class="btn btn-default" {{action 'buttonClicked' item}} > Contribute</button> -->
        </div>
    </div>
</div>

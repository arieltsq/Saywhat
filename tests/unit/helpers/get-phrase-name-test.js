import { getPhraseName } from 'frontend/helpers/get-phrase-name';
import { module, test } from 'qunit';

module('Unit | Helper | get phrase name');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = getPhraseName([42]);
  assert.ok(result);
});

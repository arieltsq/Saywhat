import { autocomplete } from 'frontend/helpers/autocomplete';
import { module, test } from 'qunit';

module('Unit | Helper | autocomplete');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = autocomplete([42]);
  assert.ok(result);
});
